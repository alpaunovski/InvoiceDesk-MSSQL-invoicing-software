<Window x:Class="InvoiceDesk.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Sign in to InvoiceDesk"
        Height="320"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Icon="/Resources/AppIcon.ico">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0 0 8 12" VerticalAlignment="Center" Text="Email" />
        <TextBox Grid.Row="0" Grid.Column="1" Width="240" Margin="0 0 0 12" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0 0 8 12" VerticalAlignment="Center" Text="Password" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Width="240" Margin="0 0 0 12" PasswordChanged="OnPasswordChanged" />

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0 0 8 12" VerticalAlignment="Center" Text="Device name" />
        <TextBox Grid.Row="2" Grid.Column="1" Width="240" Margin="0 0 0 12" Text="{Binding DeviceName, UpdateSourceTrigger=PropertyChanged}" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0 0 0 12">
            <Button Content="Sign In" Width="120" Command="{Binding LoginCommand}" IsEnabled="{Binding IsNotBusy}" />
            <Button Content="Cancel" Width="80" Margin="8 0 0 0" Click="OnCancel" />
        </StackPanel>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Vertical">
            <ProgressBar Height="10" IsIndeterminate="True" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
            <TextBlock Margin="0 8 0 0" TextWrapping="Wrap" Foreground="Red" Text="{Binding ErrorMessage}" />
        </StackPanel>
    </Grid>
</Window>
